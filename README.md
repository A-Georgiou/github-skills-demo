# github-skills-demo
A repository demonstrating GitHub API capabilities

## Running Tests

To run the test suite, execute:

```bash
./run_tests.sh
```

The tests verify the repository structure and file contents.
